{
  "scripts": {
    "start": "npm start --prefix=superrentals/ember",
    "postinstall": "npm install --prefix=superrentals/ember",
    "heroku-postbuild": "npm run build:production --prefix=superrentals/ember"
  },
  "cacheDirectories": [
    "superrentals/ember/node_modules"
  ],
  "engines": {
    "node": "10.15.1"
  }
}
