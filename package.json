{
  "scripts": {
    "start": "npm start --prefix=example/ember",
    "postinstall": "npm install --prefix=example/ember"
  },
  "cacheDirectories": [
    "example/ember/node_modules"
  ],
  "engines": {
    "node": "10.15.1"
  }
}
