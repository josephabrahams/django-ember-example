{
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix=superrentals/ember",
    "build": "npm run build:production --prefix=superrentals/ember",
    "test": "npm run lint --prefix=superrentals/ember && npm test --prefix=superrentals/ember",
    "start": "npm start --prefix=superrentals/ember"
  },
  "cacheDirectories": [
    "superrentals/ember/node_modules"
  ],
  "engines": {
    "node": "10.15"
  }
}
